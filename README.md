# affint-preprocessing gear

This gear does basic preprocessing using confound regressors created by fmriprep to remove noise from the raw data. It requires that data are first pre-processed with fmriprep. 